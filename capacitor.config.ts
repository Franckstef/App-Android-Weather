import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projet1-Meteo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
